# sm-onplayerruncmd-pre-evil-dontuse
Lmao

Hooks directly into SDKTools and DHooks CHookMgr::PlayerRunCmd to get the `CUserCmd ucmd*` before any plugins have any chance to modify values in `OnPlayerRunCmd`
